# Actions package

**Status: Approved / uploaded.**

Production archive:

```text
docs/06-ui/assets/packages/actions/TourCRM_Assets_Actions_P0_FINAL.zip
```

Scope: 17 approved action icons, each exported as WebP at `16`, `20`, `24`, `32`, `48`, `64` px (102 production WebP assets).

Catalog:

`add`, `edit`, `delete`, `archive`, `restore`, `search`, `filter`, `sort`, `save`, `cancel`, `confirm`, `close`, `back`, `forward`, `more`, `download`, `upload`.

Future application integration root:

```text
assets/ui/icons/actions/
```

Use the approved production artwork from the final package. Do not replace it with generic icon-library equivalents without an explicit design decision.

See `../../ASSET-PACKS.md`, `../../ASSET-STATUS.md` and `../../asset-manifest.json` for the complete contract.
