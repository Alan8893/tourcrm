# Status package

**Status: Approved / uploaded.**

Production archive:

```text
docs/06-ui/assets/packages/status/TourCRM_Assets_Status_P0_FINAL.zip
```

Scope: 9 approved semantic status icons, each exported as WebP at `16`, `20`, `24`, `32`, `48`, `64` px (54 production WebP assets).

Catalog:

`planned`, `ongoing`, `completed`, `ended`, `archived`, `success`, `warning`, `error`, `info`.

Future application integration root:

```text
assets/ui/icons/status/
```

`ended` is the approved extinguished-campfire metaphor. Do not add new status meanings in this batch.

See `../../ASSET-PACKS.md`, `../../ASSET-STATUS.md` and `../../asset-manifest.json` for the complete contract.
