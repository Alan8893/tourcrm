"""RBAC + permission scope enforcement (Issue #29).

See app.authorization.service for the reusable `can()`/`Authorizer`
mechanism and app.authorization.context for the scope vocabulary and the
explicit, backend-asserted ResourceContext it evaluates against.
"""
